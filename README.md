# MP Entertainment (mpentertainment1)

Next.js (App Router) + TypeScript + Tailwind project scaffold.

## Local dev
```bash
npm i
npm run dev
```

## Notes
- Mock data lives in `app/_data/*`
- Public pages: `/`, `/announcements`, `/events`, `/tickets`, ...
- Admin: `/admin`
- API routes: `/api/*` (mock)
